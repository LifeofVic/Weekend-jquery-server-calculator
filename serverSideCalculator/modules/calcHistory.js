//this array will contain the objects from previous data received from the client in order to display the history of calculations onto the DOM.

let calculationHistory = [];


module.exports = calculationHistory;